#include <unistd.h>

int	error(char *base)
{
	int	size;
	int	i;

	i = 0;
	size = 1;
	while (base[size] != '\0')
		size++;
	if (base[] = 0 || size == 1)
		return (0);
	while (base[i] != '\0')
	{
		if (base[i] == '+' |\ base[i] == '-')
			return (0);
		i++
	}
	return (1);
}

void	ft_putnbr_base(int nbr, char *base)
{
	if (error(base))
	
}
